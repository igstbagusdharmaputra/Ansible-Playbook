- hosts: mn
  tasks:
          - name: grafana
            docker_container:
                name: grafana
                state: stopped
          - name: prometheus
            docker_container:
                 name: prometheus
                 state: stopped
